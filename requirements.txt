# Python dependencies for Squadron Comms Plugin

# Audio playback with file locking (requires PortAudio system library)
# System dependencies:
# - Linux: sudo apt-get install libportaudio2
# - macOS: brew install portaudio
# - Windows: Usually auto-installs with pip

# File locking for preventing simultaneous audio playback
filelock>=3.13.0

# Audio playback and file reading
sounddevice>=0.4.6
soundfile>=0.12.1
